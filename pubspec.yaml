name: gover_front
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # google_maps_flutter: ^2.1.1  # Uncomment if you need Google Maps
  flutter_map: ^3.1.0  # Latest version for Flutter Map
  latlong2: ^0.8.1
  intl: ^0.17.0
  image_picker: ^0.8.7+3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo_green.png
